# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
*.swo

# Pila run artifacts — created per-run in target repos, never tracked.
.pila/
# Top-level log when the CLI is teed (`./pila "..." | tee pila.log`).
pila.log

# Local Claude Code settings / cache — per-machine, never tracked.
.claude/

# Working notes — kept locally as a reference, not a deliverable.
foo.txt

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
